set(XPCC_LINKER_SCRIPT "${CMAKE_CURRENT_LIST_DIR}/ld/linkscript.ld.in" CACHE INTERNAL "")    

IF(NOT LINKER_FLASH_ORIGIN)
SET(LINKER_FLASH_ORIGIN "0x08000000")
endif()

IF(NOT LINKER_FLASH_LENGTH)
SET(LINKER_FLASH_LENGTH "512k")
endif()

IF(NOT LINKER_RAM1_ORIGIN)
SET(LINKER_RAM1_ORIGIN "0x20000000")
endif()

IF(NOT LINKER_RAM1_LENGTH)
SET(LINKER_RAM1_LENGTH "128k")
endif()

IF(NOT LINKER_RAM2_ORIGIN)
SET(LINKER_RAM2_ORIGIN "0x2001C000")
endif()

IF(NOT LINKER_RAM2_LENGTH)
SET(LINKER_RAM2_LENGTH "16k")
endif()

IF(NOT LINKER_MAIN_STACK_SIZE)
SET(LINKER_MAIN_STACK_SIZE "2048")
endif()

IF(NOT LINKER_PROCESS_STACK_SIZE)
SET(LINKER_PROCESS_STACK_SIZE "8192")
endif()